---
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
    theme: yeti
    css: "ReportDashboardStyles.css"
editor_options: 
  chunk_output_type: console
---
### Synthesized Firms

```{r Synthesized_Firms_ValueBox}
value <- format(ScenarioFirms[TAZ_TYPE == "SEMCOG", .N], big.mark = ",", digits = 0, scientific = FALSE, trim = TRUE)
valueBox(value, caption = "Synthesized Firms (SEMCOG)", icon = "fa-building-o")
```

### Employment

```{r Employment_ValueBox}
value <- format(ScenarioFirms[TAZ_TYPE == "SEMCOG", sum(Employees)], big.mark = ",", digits = 0, scientific = FALSE, trim = TRUE)
valueBox(value, caption = "Employment (SEMCOG)", icon = "fa-building-o")
```